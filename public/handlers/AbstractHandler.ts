import { Candidate } from './../Candidate';

export class AbstractHandler {

  public Perform(cadidate: Candidate, target: Uint8Array) {
    // let result: Uint8Array = target;
    // base.Perform(candidate, target);

    // if (target == null) {
    //   result = this.ConvertFileToByteArray(candiate);
    // } else {
    //   this.ConvertByteArrayToFile(candidate, target);
    // }
    // return result;
  }
}